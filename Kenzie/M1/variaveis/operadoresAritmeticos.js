// 1- Declare uma variável chamada num1 e atribua a ela o valor 5. Em seguida, declare uma variável chamada num2 e atribua a ela o valor 10. Adicione as duas variáveis e imprima o resultado no console.

let num1 = 5;
let num2 = 10;
result = num1 + num2;
console.log(result);

// 2- Declare uma variável chamada pi e atribua a ela o valor de 3.14. Em seguida, declare uma variável chamada raio e atribua a ela o valor 5. Calcule o comprimento da circunferência com base no raio e imprima o resultado no console. (dica: multiplique o raio pelo valor de pi e pelo numero dois)

let pi = 3.14;
let raio = 5;
result = raio * pi * 2;
console.log(result);

// Declare uma variável chamada temperatura e atribua a ela o valor 30. Em seguida, converta a temperatura de Celsius para Fahrenheit e imprima o resultado no console. (dica: Multiplique a temperatura por nove, após isso divida esse valor por cinco e adicione trinta e dois para converter de Celsius para Fahrenheit).

let temperatura = 30;
result = (temperatura * 9) / 5 + 32;
console.log(result);

// 4- Declare uma variável chamada nota1 e atribua a ela o valor 7. Em seguida, declare uma variável chamada nota2 e atribua a ela o valor 9. Calcule a média das duas notas e imprima o resultado no console.

let nota1 = 7;
let nota2 = 9;
result = nota1 / nota2;
console.log(result);
// 5- Declare uma variável chamada salarioBruto e atribua a ela o valor 5000. Em seguida, calcule e imprima o salário líquido, considerando um desconto de 10% para impostos.

let salarioBruto = 5000;
salarioLiquido = 5000 * (10 / 100);
resultado = salarioBruto - salarioLiquido;
console.log(resultado);
